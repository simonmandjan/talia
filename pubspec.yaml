name: taxi_booking
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+101

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  # TOAST
  fluttertoast: ^8.2.8

  # IMAGE
  cached_network_image: ^3.3.1
  # BASE
  intl: ^0.19.0
  google_fonts: ^6.2.1
  html: ^0.15.4
  shared_preferences: ^2.3.2
  nb_utils: ^7.1.8 # <-- Mis à jour avec la version stable recommandée par Flutter

  # UI
  scratcher: ^2.5.0
  lottie: ^3.3.1
  flutter_rating_bar: ^4.0.1
  flutter_staggered_animations: ^1.1.1
  country_code_picker: ^3.0.0
  flutter_native_contact_picker: ^0.0.10
  dotted_border: ^2.1.0
  dotted_line: ^3.2.2
  timeline_tile: ^2.0.0
  flutter_vector_icons: ^2.0.0
  permission_handler: ^11.3.1
  pinput: ^5.0.0
  # MAP
  geolocator: ^10.0.1
  geocoding: ^3.0.0
  google_maps_flutter: ^2.9.0
  # IMAGES
  image_picker: ^1.1.2
  # CONNECTIVITY
  package_info_plus: ^8.0.2
  # CONNECTIVITY
  http: ^1.2.2
  connectivity_plus: ^6.0.5
  url_launcher: ^6.3.0
  internet_file: ^1.2.0
  #FIREBASE
  firebase_core: ^4.1.1
  firebase_analytics: ^12.0.2
  firebase_crashlytics: ^5.0.2
  cloud_firestore: ^6.0.2
  firebase_storage: ^13.0.2
  firebase_messaging: ^16.0.2
  firebase_auth: ^6.1.0
  firebase_database: ^12.0.2
  paginate_firestore:
    git:
      url: https://github.com/Gosplex/paginate_firestore.git
  # STATE MANAGEMENT
  mobx: ^2.3.3+2
  flutter_mobx: ^2.2.1+1
  # Payment gateway
  razorpay_flutter: ^1.3.7
  flutter_stripe: ^11.0.0
  flutterwave_standard_smart: ^1.0.4
  flutter_paytabs_bridge: ^2.6.17
  my_fatoorah_payment_flutter: ^1.1.3
  flutter_inappwebview: any # <-- Spécifié 'any' pour éviter l'erreur de syntaxe vide
  flutter_paystack:
    git:
      url: https://github.com/appmeetmighty/flutter_paystack.git
  # PUSH NOTIFICATION
  onesignal_flutter: ^5.2.4
  # Social Login
  google_sign_in: ^6.2.1
  #  the_apple_sign_in: ^1.1.1
  sign_in_with_apple: ^7.0.1
  crypto: any
  # DOWNLOAD INVOICE PDF
  pdf: ^3.11.1
  pdfx: 2.6.0
  path_provider: ^2.1.4
  open_file: ^3.5.3
  share_plus: any # <-- Spécifié 'any' pour éviter l'erreur de syntaxe vide
  timezone: ^0.9.2

  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.11
  mobx_codegen: ^2.6.1
  very_good_analysis: ^9.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.7

dependency_overrides:
  package_info_plus: ^4.1.0
  stripe_android: 11.4.0
  meta: ^1.15.0
  intl: ^0.18.0
  http: ^0.13.5

flutter:
  uses-material-design: true

  assets:
    - images/
    - images/app_images/
    - images/json_files/
    - images/icons/
    - staticjson/
    - staticjson/keyword_list.json

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "images/app_images/ic_app_logo.png"
  min_sdk_android: 21

flutter_native_splash:
  color: "#312783"
  image: "images/app_images/ic_app_logo.png"
  android_12:
    image: "images/app_images/ic_app_logo.png"
    color: "#312783"
  fullscreen: true